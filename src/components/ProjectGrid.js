import styled from 'styled-components'
import Block from './Block'

const StyledProjectGrid = styled(Block)`
  background-color: #ffae50;
  text-align: center;
  font-style: italic;
  font-family: Georgia, serif;
`

export default StyledProjectGrid;
